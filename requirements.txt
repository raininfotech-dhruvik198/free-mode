# torch installation (including CUDA version) is handled in cog.yaml
diffusers==0.27.2
transformers==4.39.3
accelerate==0.29.3
gradio==4.25.0
numpy==1.26.4
Pillow==10.3.0
einops==0.7.0
# spaces is a Hugging Face specific library, not typically needed for Cog/Replicate
# If FluxKontextPipeline or its components have other specific hidden dependencies,
# they would need to be added here. For now, these are the main ones.
