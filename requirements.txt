torch==2.2.2+cu121 # Explicitly add torch with CUDA for Replicate build
diffusers==0.34.0
transformers==4.39.3
accelerate==0.29.3
huggingface-hub==0.22.2 # Added to resolve ImportError for cached_download
gradio==4.25.0
numpy==1.26.4
Pillow==10.3.0
einops==0.7.0
# spaces is a Hugging Face specific library, not typically needed for Cog/Replicate
# If FluxKontextPipeline or its components have other specific hidden dependencies,
# they would need to be added here. For now, these are the main ones.
